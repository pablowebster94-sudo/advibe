import {NextResponse} from "next/server";
import {getAdminSession} from "@/lib/enfoque-admin";
import {supabaseAdmin} from "@/lib/enfoque-supabase";
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){const s=await getAdminSession();if(!s)return NextResponse.json({error:"No autorizado"},{status:401});try{const {id}=await params;const b=await req.json();const rows=await supabaseAdmin<any[]>(`listing_images?id=eq.${encodeURIComponent(id)}&select=*`,{},s.token);const item=rows[0];if(!item)return NextResponse.json({error:"Imagen no encontrada."},{status:404});if(b.is_cover){const field=item.property_id?"property_id":"vehicle_id";const parent=item.property_id||item.vehicle_id;await supabaseAdmin(`listing_images?${field}=eq.${encodeURIComponent(parent)}&is_cover=eq.true`,{method:"PATCH",headers:{"Prefer":"return=minimal"},body:JSON.stringify({is_cover:false})},s.token)}await supabaseAdmin(`listing_images?id=eq.${encodeURIComponent(id)}`,{method:"PATCH",headers:{"Prefer":"return=minimal"},body:JSON.stringify({is_cover:Boolean(b.is_cover),sort_order:Number(b.sort_order||0)})},s.token);return NextResponse.json({ok:true})}catch{return NextResponse.json({error:"No se pudo actualizar la imagen."},{status:500})}}
