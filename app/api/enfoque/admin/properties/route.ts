import {NextResponse} from "next/server";import {getAdminSession} from "@/lib/enfoque-admin";import {supabaseAdmin} from "@/lib/enfoque-supabase";
async function owner(token:string){const found=await supabaseAdmin<any[]>("owners?name=eq.AdVibe%20Agencia&kind=eq.propio&limit=1",{},token);if(found[0])return found[0].id;const rows=await supabaseAdmin<any[]>("owners",{method:"POST",headers:{"Prefer":"return=representation"},body:JSON.stringify({name:"AdVibe Agencia",kind:"propio"})},token);return rows[0].id;}
export async function POST(req:Request){const s=await getAdminSession();if(!s)return NextResponse.json({error:"No autorizado"},{status:401});try{const b=await req.json();if(!b.title||!b.slug||!b.price||!b.city)return NextResponse.json({error:"Título, slug, precio y ciudad son obligatorios."},{status:400});const rows=await supabaseAdmin<any[]>("properties",{method:"POST",headers:{"Prefer":"return=representation"},body:JSON.stringify({slug:b.slug,title:b.title,description:b.description||"",price:Number(b.price),currency:"USD",property_type:b.property_type||"casa",operation_type:b.operation_type||"venta",city:b.city,province:b.province||"Azuay",sector:b.sector||null,features:Array.isArray(b.features)?b.features:[],owner_id:await owner(s.token),publication_status:b.publication_status||"borrador",availability:"disponible",is_featured:Boolean(b.is_featured)})},s.token);return NextResponse.json({ok:true,item:rows[0]});}catch(e){return NextResponse.json({error:"No se pudo crear la propiedad."},{status:500})}}
