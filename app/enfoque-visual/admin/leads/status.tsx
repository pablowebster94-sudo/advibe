"use client";import {useState} from "react";export function LeadStatus({id,initial}:{id:string;initial:string}){const [status,setStatus]=useState(initial);async function change(v:string){setStatus(v);await fetch("/api/enfoque/admin/leads/"+id,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({status:v})});}return <select value={status} onChange={e=>change(e.target.value)} className="rounded-lg bg-black/5 px-2 py-2 text-xs"><option>nuevo</option><option>contactado</option><option>calificado</option><option>visita_agendada</option><option>negociacion</option><option>cerrado</option><option>descartado</option></select>}
