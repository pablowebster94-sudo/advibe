"use client";
import {useState} from "react";import {useRouter} from "next/navigation";
export default function Login(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const [busy,setBusy]=useState(false);const router=useRouter();
async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError("");const r=await fetch("/api/enfoque/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});const d=await r.json();if(!r.ok){setError(d.error||"No se pudo iniciar sesión");setBusy(false);return}router.push("/admin");router.refresh();}
return <main className="min-h-screen bg-black px-5 py-20 text-white"><div className="mx-auto max-w-md"><p className="text-xs font-black uppercase tracking-[.25em] text-[#d9ff3f]">Enfoque Visual</p><h1 className="ev-display mt-4 text-6xl font-black">Panel.</h1><form onSubmit={submit} className="mt-10 space-y-4"><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Correo" className="w-full rounded-2xl bg-white/10 px-4 py-4 outline-none"/><input required type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Contraseña" className="w-full rounded-2xl bg-white/10 px-4 py-4 outline-none"/>{error&&<p className="text-sm text-red-300">{error}</p>}<button disabled={busy} className="w-full rounded-full bg-[#d9ff3f] px-6 py-4 font-black text-black">{busy?"Entrando…":"Entrar"}</button></form></div></main>}
